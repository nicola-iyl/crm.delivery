import React, { useState, useEffect } from "react";

const SettingItem = ( { service, settings } ) => {

  const [ settings, setSettings ] = useState( [] );

  useEffect( () => {
    
  }, []);

  return (<div></div>);
}

export default SettingItem;